# Python - Inheritance

This project is part of the ALU Higher Level Programming curriculum. It
covers inheritance in Python: base classes and subclasses, overriding
methods, and the built-in functions used to inspect class relationships.

## Learning Objectives

* What a superclass, baseclass or parentclass is
* What a subclass is
* How to list all attributes and methods of a class or instance
* When an instance can have new attributes
* How to inherit a class from another
* How to define a class with multiple base classes
* The default class every class inherits from
* How to override a method or attribute inherited from the base class
* Which attributes and methods are available by heritage to subclasses
* The purpose of inheritance
* When and how to use isinstance, issubclass, type and super

## Requirements

* Allowed editors: `vi`, `vim`, `emacs`
* Interpreted on Ubuntu 20.04 LTS using python3 (version 3.8.5)
* All files end with a new line and are executable
* The first line of all files is exactly `#!/usr/bin/python3`
* Code follows the `pycodestyle` style guide (version 2.7.*)
* All modules, classes and methods are documented
* Test files live in the `tests` folder and run with
  `python3 -m doctest ./tests/*`

## Tasks

| File | Description |
| ---- | ----------- |
| `0-lookup.py` | Returns the list of attributes and methods of an object |
| `1-my_list.py` | MyList class inheriting from list, with print_sorted |
| `2-is_same_class.py` | True if an object is exactly an instance of a class |
| `3-is_kind_of_class.py` | True if an object is an instance or a subclass instance |
| `4-inherits_from.py` | True if an object inherits from a given class |
| `5-base_geometry.py` | An empty BaseGeometry class |
| `6-base_geometry.py` | BaseGeometry with an unimplemented area method |
| `7-base_geometry.py` | BaseGeometry with an integer_validator method |
| `8-rectangle.py` | Rectangle inheriting from BaseGeometry |
| `9-rectangle.py` | Rectangle with area and string representation |
| `10-square.py` | Square inheriting from Rectangle |
| `11-square.py` | Square with its own string representation |

## Author

Clovis — ALU Higher Level Programming
